export enum orderStatuses {
  PENDING = 'PENDING',
  PROCESSING = 'PROCESSING',
  CANCELLED = 'CANCELLED',
  DELIVERED = 'DELIVERED',
  RETURNED = 'RETURNED',
}

export enum paymentStatuses {
  UNPAID = 'UNPAID',
  PAID = 'PAID',
}
