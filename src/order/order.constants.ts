export enum orderStatuses {
  PENDING = 'PENDING',
  PROCESSING = 'PROCESSING',
  SHIPPING = 'SHIPPING',
  CANCELLED = 'CANCELLED',
  DELIVERED = 'DELIVERED',
  RETURNED = 'RETURNED',
}

export enum paymentStatuses {
  PENDING = 'PENDING',
  PAID = 'PAID',
  FAILED = 'FAILED',
}
