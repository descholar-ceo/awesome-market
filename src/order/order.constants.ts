export enum orderStatuses {
  PENDING = 'PENDING',
  PROCESSING = 'PROCESSING',
  SHIPPING = 'SHIPPING',
  CANCELLED = 'CANCELLED',
  DELIVERED = 'DELIVERED',
  RETURNED = 'RETURNED',
}

export enum paymentStatuses {
  UNPAID = 'PENDING',
  PAID = 'PAID',
  FAILED = 'FAILED',
}
