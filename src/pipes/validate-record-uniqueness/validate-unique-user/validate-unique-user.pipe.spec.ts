import { ValidateUniqueUserPipe } from './validate-unique-user.pipe';

describe('ValidateUniqueUserPipe', () => {
  it('should be defined', () => {
    expect(new ValidateUniqueUserPipe()).toBeDefined();
  });
});
