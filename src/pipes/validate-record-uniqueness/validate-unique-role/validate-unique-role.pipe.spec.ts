import { ValidateUniqueRolePipe } from './validate-unique-role.pipe';

describe('ValidateUniqueRolePipe', () => {
  it('should be defined', () => {
    expect(new ValidateUniqueRolePipe()).toBeDefined();
  });
});
